//import { useTheme } from '@mui/material/styles';

const LoginStyles = () => {
  //const theme = useTheme();

  return {
    container: {
      marginTop: "10vh",
    }
  };
};

export default LoginStyles;
